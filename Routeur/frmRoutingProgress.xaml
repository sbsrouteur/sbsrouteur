<Window
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	x:Class="frmRoutingProgress"
	x:Name="Window"
	Title="{Binding Title, Mode=Default}" mc:Ignorable="d" ResizeMode="NoResize" Icon="Graphics/yacht.ico" WindowStyle="ToolWindow">

	<Grid x:Name="LayoutRoot">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto"/>
			<ColumnDefinition Width="1*"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="auto"/>
			<RowDefinition Height="auto"/>
		</Grid.RowDefinitions>
		<ProgressBar Grid.ColumnSpan="2" Height="18	" d:Value="23" Value="{Binding ProgressValue, Mode=OneWay}"/>
		<TextBlock Text="Complete in :" TextWrapping="Wrap" Grid.Row="1" Margin="{DynamicResource DefMargin}"/>
		<TextBlock Text="{Binding ProgressETA, Mode=Default}" TextWrapping="Wrap" Grid.Column="1" Grid.Row="1" Margin="{DynamicResource DefMargin}"/>
	</Grid>
</Window>